{
  "name": "@chessviewer/types",
  "version": "0.0.1",
  "description": "Shared types between packages",
  "types": "src/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b",
    "lint": "eslint src",
    "clean": "rm -rf *.tsbuildinfo dist"
  },
  "author": "Daniel Walters",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.1.6"
  }
}
